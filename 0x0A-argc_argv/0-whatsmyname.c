#include "main.h"
#include <stdio.h>
/**
 * main - prints its name + \n
 * @argc: number of agruments typed
 * @argv: array pointing to arguements
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}